# 数据库配置
DATABASE_URL=sqlite+aiosqlite:///./email_admin.db

# 加密密钥 (生产环境请使用安全的随机密钥)
# 可以使用以下命令生成: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# 服务器配置
HOST=0.0.0.0
PORT=8655
DEBUG=true
